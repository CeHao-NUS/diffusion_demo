import torch
import torch.optim as optim
from torch.distributions import MultivariateNormal



def line_optimize(input_x, mask, const_value, ):
    # 1. from data, get the mean and covariance

    # 2. from the mean and covariance, get the distribution

    # 3. calculate mask, mse loss

    # 4. calculate the NLL loss

    # 5. optimize the loss

    # 6. return the optimized value

    pass